# DATASOURCE
spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        id:
          new_generator_mappings: false
        format_sql: true
  datasource:
    jdbcUrl: jdbc:h2:mem:test-db;DB_CLOSE_DELAY=-1
